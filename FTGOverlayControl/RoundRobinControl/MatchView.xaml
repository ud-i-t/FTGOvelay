<UserControl x:Class="RoundRobinControl.MatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoundRobinControl"
             mc:Ignorable="d"
             d:Height="60" d:Width="600">
    <StackPanel Orientation="Horizontal">
        <Button Width="120" Background="{Binding Player1Color}" Command="{Binding OnPlayer1Win}">Win</Button>
        <TextBlock FontSize="20" Width="200" TextAlignment="Right" Margin="20 0 20 0" Text="{Binding Player1Name}"></TextBlock>
        <TextBlock FontSize="20">vs</TextBlock>
        <TextBlock FontSize="20" Width="200" TextAlignment="Left" Margin="20 0 20 0" Text="{Binding Player2Name}"></TextBlock>
        <Button Width="120" Background="{Binding Player2Color}" Command="{Binding OnPlayer2Win}">Win</Button>
    </StackPanel>
</UserControl>
