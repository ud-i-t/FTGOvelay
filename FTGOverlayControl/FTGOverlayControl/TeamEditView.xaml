<Window x:Class="FTGOverlayControl.TeamEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FTGOverlayControl"
        mc:Ignorable="d"
        Title="TeamEditView" Height="800" Width="640">
    <StackPanel>
        <ComboBox ItemsSource="{Binding Teams}" SelectedItem="{Binding CurrentTeam}" DisplayMemberPath="Name"></ComboBox>
        <ItemsControl ItemsSource="{Binding CurrentPlayers}" IsTabStop="False">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Width="100">ポジション名</TextBlock>
                        <TextBox Width="200" Text="{Binding Name}"></TextBox>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</Window>
